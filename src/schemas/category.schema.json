{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["label"],
  "properties": {
    "label": {
      "type": "string",
      "description": "Display name for this category in the sidebar"
    },
    "pages": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Ordered list of filenames (no extension) and subfolder names. When omitted, items are auto-discovered and sorted alphabetically."
    },
    "strip": {
      "type": "boolean",
      "description": "When true, this folder name is stripped from URL slugs"
    },
    "root": {
      "type": "boolean",
      "description": "When true, promotes this category to a header tab. Only sidebar items within this folder are shown when the tab is active."
    }
  },
  "additionalProperties": false
}
