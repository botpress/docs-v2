---
import Callout from './Callout.astro';

interface Props {
  title?: string;
  class?: string;
}

const { title, class: className } = Astro.props;
---
<Callout variant="note" title={title} class={className}><slot /></Callout>
