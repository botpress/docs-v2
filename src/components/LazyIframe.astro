---
import { LazyIframe } from "./LazyIframe";

interface Props {
  src: string;
  title: string;
  height?: string;
  class?: string;
}

const { src, title, height, class: className } = Astro.props;
---

<LazyIframe src={src} title={title} height={height} className={className} client:load />
