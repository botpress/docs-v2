---
title: Scoping variables
---

import { Picture } from 'astro:assets'
import Frame from '@/components/Frame.astro'
import Icon from '@/components/Icon.astro'
import { GoodToKnow } from '@/components/callouts'
import newConversationImg from './assets/new-conversation.png'
import newConversationDarkImg from './assets/new-conversation-dark.png'
import variableTestImg from './assets/variable-test.png'
import variableTestDarkImg from './assets/variable-test-dark.png'
import userVariableTestImg from './assets/user-variable-test.png'
import userVariableTestDarkImg from './assets/user-variable-test-dark.png'
import userVariableImg from './assets/user-variable.png'
import userVariableDarkImg from './assets/user-variable-dark.png'

Every variable has a _scope_ that determines where and when your bot can access it.

Try restarting the conversation. Select <Icon icon="rotate-ccw"/> in the Emulator, then send a message:

<Frame>
  <Picture alt="New conversation" class="block dark:hidden" src={newConversationImg} />
  <Picture alt="New conversation" class="hidden dark:block" src={newConversationDarkImg} />
</Frame>

That's not ideal—the bot asked for our name again, even though we already told it in a previous conversation.

This is because `firstName` is a _Workflow_ variable—it keeps the same value for the current Workflow, then resets when the Workflow is done its execution. Since each new conversation re-executes the Main Workflow, the value resets every time.

<GoodToKnow>Newly-created variables have the Workflow scope by default.</GoodToKnow>

## Changing a variable's scope

Let's change `firstName` to a _User_ variable:

1. In the Variables section, hover over `firstName` and select <Icon icon="pen"/> to edit it.
2. In the **Scope** drop-down, switch from **Workflow** to **User**.
3. Select **Save**.

Now, go back to the Emulator and repeat the process. Start one conversation and tell it your name:

<Frame>
  <Picture alt="Variable test" class="block dark:hidden" src={variableTestImg} />
  <Picture alt="Variable test" class="hidden dark:block" src={variableTestDarkImg} />
</Frame>

Then, select <Icon icon="rotate-ccw"/> to restart the conversation and send a message:

<Frame>
  <Picture alt="User variable test" class="block dark:hidden" src={userVariableTestImg} />
  <Picture alt="User variable test" class="hidden dark:block" src={userVariableTestDarkImg} />
</Frame>

Much better! The bot remembered the name from our previous conversation. Notice that in the Variables section, the value stayed the same:

<Frame>
  <Picture alt="User variable" class="block dark:hidden" src={userVariableImg} />
  <Picture alt="User variable" class="hidden dark:block" src={userVariableDarkImg} />
</Frame>
